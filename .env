# Variáveis de ambiente para o PalletFlowManager

# Ambiente
NODE_ENV=development

# Porta padrão (Render ignora, mas útil localmente)
PORT=5432

# URL de conexão com PostgreSQL (Render oferece isso)
     DATABASE_URL=postgres://postgres:admin123@localhost:5432/palletflow

# Chave secreta para sessões ou autenticação (se usar)
SESSION_SECRET=your_secret_key

# Outras variáveis específicas podem ser adicionadas conforme o código exigir
